# Radar de pasajes: panel

Interfaz web para configurar las vigilancias del radar de pasajes y ver las ofertas.
No contiene datos personales: se conecta a tu repositorio privado con un token que queda guardado solo en tu navegador.

Se publica con GitHub Pages (Settings → Pages → rama `main`, carpeta `/ (root)`).
